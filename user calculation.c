#include<stdio.h>

int main()
{
    int fis, sec, sum;
    printf("\nenter the input of first integer : ");
    scanf("%d", &fis);
    printf("\nenter the input of second integer : ");
    scanf("%d", &sec);
    sum = fis + sec ;
    printf("sum = %d", sum);    


    return 0;
}